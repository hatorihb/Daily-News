name: Tweet Daily Report

on:
  push:
    branches: [main]
    paths:
      - 'reports/**.html'

permissions:
  contents: write

jobs:
  tweet:
    runs-on: ubuntu-latest
    steps:
      - name: X posting paused
        run: echo "X posting is currently paused. Skipping."
